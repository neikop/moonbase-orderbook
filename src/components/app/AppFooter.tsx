import { Container, Flex } from "@chakra-ui/react"

const AppFooter = () => {
  return (
    <Container borderTopWidth={1} fluid px={4}>
      <Flex alignItems="stretch" h={14} justifyContent="space-between">
        {/*  */}
        {/*  */}
      </Flex>
    </Container>
  )
}

export default AppFooter
